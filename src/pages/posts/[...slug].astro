---
// This route exists so Astro can validate the dynamic /blogs/* -> /posts/* redirect.
// Concrete post pages are generated from the Markdown files in this directory.
export function getStaticPaths() {
  return [];
}
---
